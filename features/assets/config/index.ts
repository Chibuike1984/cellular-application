import type { IconProps } from "type";

const ASSET_CONFIG = {
  className: "hover:text-white",
  width: 20,
  height: 20,
  stroke: "currentColor",
  fill: "currentColor",
} satisfies IconProps;

export { ASSET_CONFIG };
